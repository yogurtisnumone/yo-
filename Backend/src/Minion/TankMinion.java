package src.Minion;

import src.Strategy.Strategy;
import src.Position.Position;
import src.Player.Player;

public class TankMinion extends Minion{//finish
    public TankMinion(String name, int defense, Strategy strategy, Position position, Player player) {
        super(name,defense,strategy,position,player);
    }
}
